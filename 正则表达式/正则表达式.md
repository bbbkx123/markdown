* ```^```在```[]```是表示否定, 在```()```是表示开头
* ```(?!pattern)``` 负向先行断言

```js
// 例子
const reg = /x\=(?!25)/
const str = 'x=25'
console.log(reg.test(str))
```


```js

// 测试题1 - ?!负向先行断言
// 匹配非0的10位整数位和最多2位的小数位的数字
const reg1 = /^([+]?|[-]?)(?!0+$)(?!0*\.0*$)\d{1,10}(\.\d{1,2})?$/;

// 测试题2 - 匹配链接
// 匹配参数
const str2 = 'https://www.baidu.com?name=jawil&age=23'
// [^&]* 表示匹配到字符&为止
const reg2 = /\?name=([^&]*)/

// 测试题3 - 密码强度
// 至少6位, 一个大写字母, 一个小写字母, 一个数字, 一个特殊符号
const str3 = ''
// !@#$%^&*? 
const reg3 = /^.*(?=.{6,})(?=.*\d)(?=.*[A-Z])(?=.*[a-z])(?=.*[!@#$%^&*?]).*$/

// 测试题4 - 匹配链接
// 请通过正则表达式，把 () => 1+1 修改为 function () { return 1+1 }
const str4 = '() => 1+1'

const reg4 = /(.*)\s=>\s(1\+1)/

```


